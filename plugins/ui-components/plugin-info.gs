if(!initializers) var initializers = [];

initializers.push({
  'function':function(){
    var pluginInfo = {
      'name':'ingenium-plugin-ui-components',
      'version':1.0
    };
    registerPlugin(pluginInfo);
  }
});